import { createTheme } from "@mui/material";

export  const theme = createTheme({
    palette: {
      text: {
        primary: "#00ff00",
        secondary: "#rrr",
      },
    },
    typography: {
      allVariants: {
        color: "#00ff00",
      },
    },
  });
