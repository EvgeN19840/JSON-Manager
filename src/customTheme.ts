import { createTheme } from "@mui/material";


export  const theme = createTheme({
    palette: {
      text: {
        primary: "#2196f3",
        secondary: 'yellow'
      },
    },
    typography: {
      allVariants: {
        color:"black"
      },
    },
  });
